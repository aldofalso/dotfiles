#!/bin/sh
# powermenu

powermenu="\
zzzz		systemctl suspend
lock		slock	
reboot		systemctl reboot 
shutdown	systemctl poweroff"

choice="$(echo "$powermenu" | cut -d'	' -f 1 | dmenu -i -fn monospace:size=12)" 

`echo "$powermenu" | grep "^$choice	" | cut -d '	' -f2-`
